title: Dagre D3 Graph

Graph(d3/dagre)

# {} -> @options Graph
'body'           -> selector BodyEl(dom/querySelector)
BodyEl selection -> element Graph

# ok, still not done, need to make chix-runtime into nodes.
setNode Graph
setEdge Graph
